[project]
name = "bot"
version = "0.1.0"
description = "LMS Telegram bot scaffold"
authors = [{ name = "Bot", email = "bot@example.com" }]
dependencies = [
    "python-dotenv>=1.0.1",
    "python-telegram-bot>=21.0.1",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
